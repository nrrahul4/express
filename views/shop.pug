extends layouts/main-layout.pug

block contents
    main
        if prods.length > 0
            each product in prods
                h1 Welcome to shop page
                p #{product.title}
        else
            h1 No Products